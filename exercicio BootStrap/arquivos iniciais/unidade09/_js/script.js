$(function() {
    $('.carousel').carousel({
        interval: 3000,
        pause: "null", // quando o mouse ficar em cima da foto, não ira ter efeito
        wrap: true // para a foto continuar passando para a proxima
    })
})